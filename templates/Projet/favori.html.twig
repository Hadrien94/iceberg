{% extends "layout.html.twig" %}

{% block page_title %}
   Mes favoris
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block content %}

    <div class="container" style="padding-top:50px;">



        {% include 'Components/_modalProfil.html.twig' %}


        {% if favoris is not empty %}
        {% for favori in favoris %}
            <!-- ##### Card ##### -->
            <div class="titre" class="row">
                <h3 class="linear-gradient">
                    {% for domaine in favori.domaine %}
                        {{ domaine.categorie }}
                    {% endfor %}
                </h3>
            </div>
            <div class="row">
                <div class="col-md-10 no-padding lib-item" data-category="view">
                    <div class="lib-panel">
                        <div class="row box-shadow">
                            <div class="col-md-3">
                                <img class="lib-img-show card-img" src="{{ asset('images/' ~ favori.image ) }}">
                            </div>   <!--{# fin div. col-md-4#}  -->

                            <div class="col-md-8">
                                <div class="lib-row lib-header">
                                    <h4>{{ favori.titre }}</h4>
                                    <div class="lib-header-seperator"></div>
                                </div>
                                <div class="lib-row lib-desc">
                                    <p>{{ favori.description | raw }}</p>
                                    <a href="">{{ favori.user.prenom }} {{ favori.user.nom }}</a>
                                    <a href="{{ path('projet_supprimer_favori', {id: favori.id}) }}"><i class="far fa-trash-alt"></i></a>
                                </div>
                            </div>   <!--{# fin div. col-md-8#} -->
                        </div>    <!--{# div.box shadow #} -->
                    </div>    <!--{# div.lib panel #}  -->
                </div>   <!--{ # div.col md-10 #}  -->
                <div class="col-md-1"></div>
            </div>  <!--{ # div.row #} -->
        {% endfor %}

        {% else %}
            <div class="alert alert-info">Vous n'avez pas encore de favori</div>

        {% endif %}


    </div> {# fin div.container #}


{% endblock %}